services:
  - type: web
    name: skill-tracker-api
    env: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: skill-tracker-db
          property: connectionString

databases:
  - name: skill-tracker-db
    databaseName: skilltracker
    user: trackeruser
    plan: free
